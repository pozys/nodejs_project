<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Главная</title>
</head>

<body>
    <div>
        <h3>Выберите группу новостей</h3>
        <form action="/news/selected" method="POST">
            <select name="news_type">
                {{#each news_types}} {{@key}}:{{this}}
                <option value="{{@key}}" {{#if (selectedNewsType  ../newsRequestParams.news_type @key)}} selected {{/if}}>
                    {{this}}</option>
                {{/each}}
            </select>
            <button type="submit">Показать</button>
        </form>
    </div>
</body>

</html>