<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Главная</title>
</head>

<body>
    {{#if error}}
    <p>Произошла ошибка: {{error}}</p>
    {{else}}
    <h2>Ваш список задач</h2>
    <div>
        {{#if tasks}}
        {{#each tasks}}
        <p>
            <a href="/show/{{this.id}}">{{this.name}}</a>
        </p>
        {{/each}}
        {{else}}
        <p>Список задач пуст</p>
        {{/if}}

    </div>
    <input type="button" value="Добавить задачу" onclick="location.href='/create'">
    {{/if}}
</body>

</html>